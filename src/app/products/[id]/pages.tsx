// export default function Category({ params }: { params: { id: string } }) {
//     return (
//         <h1>ID: {params.id}</h1>
//     )
// }

export default function Contato(
  
) {
    return (
        <h1>Prametro </h1>
    )
}